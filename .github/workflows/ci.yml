name: CI with Cache

on: 
  push: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: prime-cache-73303f3  # ← EXACT NAME REQUIRED
      uses: actions/cache@v4
      with:
        path: ~/.npm
        key: cache-73303f3
        
    - name: Install dependencies
      run: npm ci


